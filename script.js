document.querySelectorAll

 document.getElementById('save-btn').addEventListener('click', function() {
    const userInput = documentElementById('nameInput').value;
    localStorage.setItem('saveButton', userInput);

 });

 //When the page is loaded or refreshed, check if a name is saved in localStorage and display it.

 localStorage.getItem(#username);


const clearButton = document.body.querySelector("#clear-btn");
clearButton.addEventListener("click", () => {
  clearButton.textContent = "";
});

 
 



